name: Deploy Images

on:
  workflow_run:
    workflows: ["Build Images"]
    branches: [main]
    types: 
      - completed
  workflow_dispatch: {}

jobs:
  deploy-images:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - run: echo 'The Build Images workflow passed, we can deploy now!'
